#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings.
 * Return: the result
 * @s1: parameter
 * @s2: parameter
*/

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
