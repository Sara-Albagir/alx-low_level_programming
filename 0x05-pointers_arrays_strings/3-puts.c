#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string, followed by a new line
 * @str: parameter
 * Return: 0 upon success
*/

void _puts(char *str)
{
	char *str;

	puts(str);
	return (0);
}
