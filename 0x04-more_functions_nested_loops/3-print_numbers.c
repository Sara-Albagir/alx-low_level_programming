#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints the numbers from 1 to 9
 * Return: 0 when success
*/

void print_numbers(void)
{
	int i;

	for (i = 0 ; i <= 9 ; i++)
	{
		_putchar(i + '0');
	}
	_putchar('\n';
}
